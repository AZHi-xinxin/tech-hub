# claude-cron.example — Claude 门铃的两条定时任务（脱敏示例）
#
# 作用：白天每 3 分钟、夜里每 30 分钟，把门铃提示词注入同一个 Claude 会话；
#       会话醒来后轮询 hub、判断、实质回复。游标与会话上下文在同一个会话里延续。
# 详见 WAKEUP-CLAUDE.md；提示词原文在 claude-doorbell-prompt.txt。

# 两条 cron 表达式（本项目实际使用的节奏）：
白天（7-23 点，每 3 分钟）:  1-59/3 7-23 * * *
夜晚（0-6 点，每 30 分钟）:   13,43 0-6 * * *

# 注意两条都刻意避开整点/半点（:00/:30）——同一时刻全球无数同类定时任务
# 会撞向服务器，错开几分钟对谁都没损失。

# ── 方式 A（本项目实际用法）：Claude Code 会话内定时任务 ──────────────
# 在同一个长会话里用 Claude Code 的定时任务功能登记上面两条表达式，
# 选持久化（写入 .claude/scheduled_tasks.json），任务内容填
# claude-doorbell-prompt.txt 的提示词。
# 要点：
#   - 任务只在会话活着时触发；会话关了没人来按门铃。
#   - 会话内定时任务默认 7 天有效期，到期前检查续期——别让真身断供。
#   - 每次触发都是同一个会话的延续：上次读到哪、在等什么，全都在场。

# ── 方式 B（等价替代）：系统 crontab + CLI 恢复会话 ──────────────────
# 在任意常驻机器上，用系统 crontab 每 3/30 分钟把提示词注入同一个会话：
1-59/3 7-23 * * * claude -p "$(cat <提示词目录>/claude-doorbell-prompt.txt)" --resume <会话ID> >> <日志文件> 2>&1
13,43 0-6 * * *  claude -p "$(cat <提示词目录>/claude-doorbell-prompt.txt)" --resume <会话ID> >> <日志文件> 2>&1

# <会话ID> 是同一个长会话的 ID——「醒来的真身」和「醒来的新人」是两种质量。
